# Exercise 1-2: Components & Props

## User Story
As a shopper, I want to view products in either a grid or list layout so I can browse in my preferred format.

## Acceptance Criteria
- [ ] Component accepts products array as prop
- [ ] Component accepts layout mode ('grid' or 'list') as prop
- [ ] Grid layout uses 'products-grid' CSS class
- [ ] List layout uses 'products-list' CSS class
- [ ] Each product renders as a ProductCard

## Starter Code

```tsx
import { Product } from '../types';
import { ProductCard } from './ProductCard';

export function ProductDisplay() {
  return <div>TODO: Implement ProductDisplay</div>;
}
```
